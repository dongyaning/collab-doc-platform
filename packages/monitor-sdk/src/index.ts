/**
 * @collab/monitor-sdk — placeholder. Real plugins land in M3.
 */
export const SDK_VERSION = '0.0.0';

export interface MonitorInitOptions {
  endpoint: string;
  release?: string;
}

export function init(_options: MonitorInitOptions): void {
  // M0 placeholder: real implementation in M3.
}
